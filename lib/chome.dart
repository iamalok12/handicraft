import 'package:flutter/material.dart';

class Chome extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Center(child: Text("Customer Home")),
        ),
      ),
    );
  }
}
